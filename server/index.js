const path = require('path');
const mali = require('mali');
const express = require('express')

function main() {
    //const gRpcServer = new mali(path.resolve(__dirname, '../proto/restaurants.proto'), 'RestaurantsService');
}

main()